 void convertToWave(int n, vector<int>& arr){
        
        // Your code here
        for(int i=0;i<arr.size();i=i+2)
        {
            if((i+1)<arr.size())
            {
                swap(arr[i],arr[i+1]);
            }
        }
        
    }
